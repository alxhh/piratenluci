# 
# Copyright (C) 2006-2008 OpenWrt.org
#
# This is free software, licensed under the GNU General Public License v2.
# See /LICENSE for more information.
#

include $(TOPDIR)/rules.mk
include $(INCLUDE_DIR)/kernel.mk

PKG_NAME:=small-ring-node
PKG_RELEASE:=1
PKG_SOURCE_URL:=@SF/small-ring-node


PKG_SOURCE:=$(PKG_NAME)-$(PKG_VERSION).tar.gz

include $(INCLUDE_DIR)/package.mk

define Package/small-ring-node
  SECTION:=net
  CATEGORY:=Base system
  TITLE:=Non-standard addons for small ring nodes
  URL:=http://localhost/trac/browser/openwrt
  DEPENDS:= \
    +olsrd-luci +olsrd-luci-mod-dyn-gw +luci-theme-openwrt\
    +ntpclient +tcpdump +ip +iputils-ping\
    +olsrd-luci-mod-txtinfo  +luci-app-olsr \
    +kmod-ipip +kmod-tun \
    +kmod-madwifi \
    +gsf-map-minimal \
    +bmxd +bmx-uci-config +bmx-gsf-map \
    +horst
    
endef

#    +ntpclient +tcpdump +wget +ip +iputils-ping\
#    +olsrd-luci-mod-txtinfo  +luci-app-olsr \
#    +libopenssl 
#    +openssl-util\

#    +kmod-usb-core +kmod-usb-net +kmod-usb-ohci +kmod-usb2 \
#    +kmod-rt73-usb +kmod-zd1211rw \


#    +kmod-madwifi \   # only makes sense for atheros minipci devices NOT brcm
#    +wprobe-export \  # I guess this one causes the crc-ccitt compile ERROR
#    +usbutils \
#    +kmod-ipip +kmod-tun \
#    +kmod-usb-core +kmod-usb-ohci +kmod-usb-uhci +kmod-usb2 \
#    +kmod-usb-net +kmod-p54-usb +kmod-rt2500-usb +kmod-rt2800-usb \
#    +kmod-rt73-usb +kmod-rtl8187  +kmod-zd1211rw\

#  KCONFIG:=CONFIG_KERNEL_KALLSYMS CONFIG_GDB


define Package/small-ring-node/description
 Addons like tcpdump, ip, wget to pull minimum set of tools into
 an embedded device image
endef



$(eval $(call BuildPackage,small-ring-node))
